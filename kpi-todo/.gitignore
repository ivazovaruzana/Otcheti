node_modules
.env
.env.*
!.env.example
.vite

# dist/ здесь НЕ игнорируется: это копия проекта внутри Otcheti,
# и kpi-todo/dist — то, что реально раздаёт GitHub Pages.
# В исходном репозитории (github.com/<owner>/kpi-todo) dist по-прежнему
# в .gitignore — там это обычный build-артефакт, а не сайт.
